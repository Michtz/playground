'use client';
import { FC } from 'react';
const SecondExercise: FC = ({}): JSX.Element => {
  const element: JSX.Element = (
    <div title="Outer div">
      <h1>Hello World!</h1>
    </div>
  );
  return <>{element}</>;
};
export default SecondExercise;
